services:
  kino:
    build: .
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file: .env
    volumes:
      - kino-data:/app/data
    environment:
      - DATABASE_URL=file:/app/data/kino.db

volumes:
  kino-data:
